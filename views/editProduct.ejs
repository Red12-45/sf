<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <!-- Link to external stylesheet -->
  <link rel="stylesheet" href="/css/editProduct.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <!-- Using Inter font for consistent typography -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="page-wrapper">
    <!-- Sidebar Navigation -->
    <aside class="sidebar">
      <div class="sidebar-header">
        <div class="logo">Edit Product</div>
    
      </div>
      <nav>
        <div class="sidebar-section">
          <div class="section-title">Navigation</div>
          <ul>
            <li>
              <a href="/"><span class="nav-icon">🏠</span>Dashboard</a>
            </li>
            <li>
              <a href="/add-product"><span class="nav-icon">➕</span> Add New Product</a>
            </li>
            <li>
              <a href="/expense"><span class="nav-icon">📜</span>Expense</a>
            </li>
            <li>
              <a href="/view-products" class="active"><span class="nav-icon">📋</span> View/Edit Products</a>
            </li>
            <li>
              <a href="/sales" class="active"><span class="nav-icon">🏷️</span>Sales</a>
            </li>
          </ul>
        </div>
        <div class="sidebar-section">
          <div class="section-title">Management</div>
          <ul>
            <li>
              <a href="/profile"><span class="nav-icon">💼</span> Profile</a>
            </li>
            <li>
              <a href="/logout"><span class="nav-icon">↪</span> Logout</a>
            </li>
          </ul>
        </div>
      </nav>
     
      <div class="sidebar-footer">
        &copy; 2025 SalesFlow
      </div>
    </aside>

    <!-- Main Content -->
    <main class="content">
      <header class="page-header">
        <h1>Edit Product</h1>
      </header>
      <section class="form-container">
        <form action="/edit-product/<%= product.id %>" method="POST">
          <!-- Product Name -->
          <div class="form-row">
            <div class="form-group">
              <label for="productName">Product Name:</label>
              <input type="text" id="productName" name="productName" value="<%= product.productName %>" required>
            </div>
          </div>
          <!-- Prices and Quantity -->
          <div class="form-row">
            <div class="form-group">
              <label for="wholesalePrice">Wholesale Price:</label>
              <input type="number" step="0.01" id="wholesalePrice" name="wholesalePrice" value="<%= product.wholesalePrice %>" required>
            </div>
            <div class="form-group">
              <label for="retailPrice">Retail Price:</label>
              <input type="number" step="0.01" id="retailPrice" name="retailPrice" value="<%= product.retailPrice %>" required>
            </div>
            <div class="form-group">
              <label for="quantity">Quantity:</label>
              <input type="number" id="quantity" name="quantity" value="<%= product.quantity %>" required>
            </div>
          </div>
          <!-- Category Selection -->
          <div class="form-row">
            <div class="form-group">
              <label for="selectedCategory">Select Category:</label>
              <select id="selectedCategory" name="selectedCategory">
                <option value="">--Choose Existing Category--</option>
                <% categories.forEach(function(cat){ %>
                  <option value="<%= cat %>" <%= (product.category === cat) ? 'selected' : '' %>><%= cat %></option>
                <% }); %>
              </select>
            </div>
            <div class="form-group">
              <label for="newCategory">Or Create New Category:</label>
              <input type="text" id="newCategory" name="newCategory" placeholder="Enter new category if not in list">
            </div>
          </div>
          <!-- Note: The product ID is not editable; it is part of the URL and remains unchanged -->
          <button type="submit">Update Product</button>
        </form>
        <br>
        <a href="/view-products" class="back-link">Back to Products List</a>
      </section>
    </main>
  </div>
  <script src="/js/firebasePersistence.js" type="module"></script>
</body>
</html>
