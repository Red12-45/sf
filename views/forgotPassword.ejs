<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password | YourBrand</title>
  <link rel="icon" href="/favicon.ico?v=<%= v %>" type="image/x-icon">
  <link rel="stylesheet" href="/css/auth.css?v=<%= v %>">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <meta name="description" content="Recover your DashInsight master-account password.">
</head>
<style>
    .info-message {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;       /* space between the text and the button */
  text-align: center;
  padding: 1rem 0; /* optional vertical padding */
}

.info-message p {
  margin: 0;       /* reset any default margins */
}

.info-message .btn-submit {
  margin: 0;       /* we’re already spacing via gap */
}
</style>
<body>
  <div class="container">
    <div class="image-container">
      <div class="overlay"></div>
      <div class="welcome-text">
        <h2>Password Recovery</h2>
        <p>Enter the email you used for the <strong>master</strong> account. We’ll send a reset link.</p>
      </div>
    </div>

    <div class="form-container">
      <div class="form-header"><h1>Forgot Password</h1></div>

      <% if (sent) { %>
        <div class="info-message">
          <p>If the email exists on a master account, a reset link is on its way!</p>
          <a href="/login" class="btn-submit">Back to Sign&nbsp;In</a>
        </div>
      <% } else { %>
        <form action="/forgot-password" method="POST" class="auth-form">
          <div class="form-group">
            <label for="email"><i class="fas fa-envelope"></i> Registered Email</label>
            <input type="email" id="email" name="email"
                   placeholder="john@example.com" required autocomplete="email">
          </div>

          <% if (error) { %>
            <div class="error-message"><%= error %></div>
          <% } %>

          <button type="submit" class="btn-submit">Send Reset Link</button>

          <div class="alternate-auth">
            <p>Remembered? <a href="/login">Sign In</a></p>
          </div>
        </form>
      <% } %>
    </div>
  </div>
</body>
</html>
