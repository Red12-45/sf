<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <title>Add New Product/Stock Batch</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <!-- Using Inter font to match styling -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/addProduct.css">
  <style>
    /* Basic inline style for hiding new product fields */
    .hidden { display: none; }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <!-- Sidebar Navigation -->
    <aside class="sidebar">
      <div class="sidebar-header">
        <div class="logo">Manage Inventory</div>
      </div>
      
      <nav>
        <div class="sidebar-section">
          <div class="section-title">Navigation</div>
          <ul>
            <li>
              <a href="/">
                <span class="nav-icon">🏠</span>
                Dashboard
              </a>
            </li>
            <li>
              <a href="/expense" class="active">
                <span class="nav-icon">📜</span>
                Expenses
              </a>
            </li>
            <li>
              <a href="/view-products">
                <span class="nav-icon">📋</span>
                View Products
              </a>
            </li>
            <li>
              <a href="/sales">
                <span class="nav-icon">🏷️</span>
                Sales
              </a>
            </li>
          </ul>
        </div>
        
        <div class="sidebar-section">
          <div class="section-title">Management</div>
          <ul>
            <li>
              <a href="/profile">
                <span class="nav-icon">💼</span>
                Profile
              </a>
            </li>
            <li>
              <a href="/settings">
                <span class="nav-icon">↪</span>
                Logout
              </a>
            </li>
          </ul>
        </div>
      </nav>
      
      <div class="sidebar-footer">
        &copy; 2025 SalesFlow
      </div>
    </aside>

    <!-- Main Content Section -->
    <main class="content">
      <header class="page-header">
        <h1>Add New Product / Stock Batch</h1>
      </header>

      <% if (success) { %>
        <div class="success-message">Product added/updated successfully!</div>
      <% } %>

      <% if (errorMessage) { %>
        <div class="error-message"><%= errorMessage %></div>
      <% } %>
      
      <!-- Filter Section for existing products -->
      <section class="filter-section">
        <form method="GET" action="/add-product">
          <label for="categoryFilter">Filter by Category:</label>
          <select name="category" id="categoryFilter">
            <option value="" <%= selectedCategory === "" ? 'selected' : '' %>>All Categories</option>
            <% categories.forEach(function(cat) { %>
              <option value="<%= cat %>" <%= selectedCategory === cat ? 'selected' : '' %>><%= cat %></option>
            <% }); %>
          </select>

          <label for="sortOrder">Sort Order:</label>
          <select name="sortOrder" id="sortOrder">
            <option value="asc" <%= sortOrder === 'asc' ? 'selected' : '' %>>A-Z</option>
            <option value="desc" <%= sortOrder === 'desc' ? 'selected' : '' %>>Z-A</option>
          </select>

          <button type="submit">Apply Filters</button>
        </form>
      </section>
      
      <!-- Main Form Section -->
      <section class="form-container">
        <div class="form-section-header">Product Information</div>
        
        <form action="/add-product" method="POST">
          <!-- Existing Product Dropdown -->
          <div class="form-row">
            <div class="form-group full-width">
              <label for="existingProduct">Select Existing Product (or choose "Add New Product")</label>
              <select id="existingProduct" name="existingProduct" onchange="toggleNewProductFields()">
                <!-- Use "new" as the value for adding a completely new product -->
                <option value="new">-- Add New Product --</option>
                <% existingProducts.forEach(function(prod) { %>
                  <option value="<%= prod.id %>"><%= prod.name %></option>
                <% }); %>
              </select>
            </div>
          </div>
          
          <!-- New Product Fields: These appear only if "Add New Product" is selected -->
          <div id="newProductFields">
            <div class="form-row">
              <div class="form-group">
                <label for="productName" class="required">Product Name</label>
                <input type="text" id="productName" name="productName" placeholder="Enter product name">
              </div>
              <div class="form-group">
                <label for="productId">Product ID</label>
                <input type="text" id="productId" name="productId" placeholder="Enter product ID (if available)">
                <div class="helper-text">Optional: Enter if this item has an existing ID</div>
              </div>
            </div>
          </div>
          
          <!-- Pricing and quantity details -->
          <div class="form-row">
            <div class="form-group">
              <label for="wholesalePrice" class="required">Purchase Price</label>
              <div class="input-group">
                <span class="input-group-prefix">₹</span>
                <input type="number" step="0.01" id="wholesalePrice" name="wholesalePrice" required placeholder="0.00" class="input-with-prefix">
              </div>
            </div>
            <div class="form-group">
              <label for="retailPrice" class="required">Retail Price</label>
              <div class="input-group">
                <span class="input-group-prefix">₹</span>
                <input type="number" step="0.01" id="retailPrice" name="retailPrice" required placeholder="0.00" class="input-with-prefix">
              </div>
            </div>
          </div>
          
          <div class="form-row">
            <div class="form-group">
              <label for="quantity" class="required">Quantity Received</label>
              <!-- Modified to allow decimals -->
              <input type="number" step="0.01" id="quantity" name="quantity" required placeholder="0">
            </div>
            <div class="form-group">
              <label for="selectedCategory">Choose Category for New Product (optional)</label>
              <select id="selectedCategory" name="selectedCategory">
                <option value="">-- Select Category --</option>
                <% categories.forEach(function(cat) { %>
                  <option value="<%= cat %>"><%= cat %></option>
                <% }); %>
              </select>
            </div>
          </div>
          
          <div class="form-row">
            <div class="form-group full-width">
              <label for="newCategory">Or Create New Category</label>
              <input type="text" id="newCategory" name="newCategory" placeholder="Create a new category">
              <div class="helper-text">Leave blank if selecting from existing categories above</div>
            </div>
          </div>
          
          <button type="submit">Add Product / Stock</button>
        </form>
      </section>
    </main>
  </div>

  <script>
    // When the page loads, ensure that the new product fields are visible only when needed.
    function toggleNewProductFields() {
      const selectElem = document.getElementById('existingProduct');
      const newProductDiv = document.getElementById('newProductFields');
      if (selectElem.value === "new") {
        newProductDiv.style.display = "block";
      } else {
        newProductDiv.style.display = "none";
      }
    }
    // Initialize on load.
    document.addEventListener('DOMContentLoaded', toggleNewProductFields);
  </script>
</body>
</html>
